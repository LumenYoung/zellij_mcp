## 1. Auto-Takeover Helper

- [x] 1.1 Add a search-and-attach helper that attaches the unique pane match in one step.
- [x] 1.2 Reject zero-match and multi-match takeover requests deterministically.

## 2. Cooperative Replace Helper

- [x] 2.1 Add a helper that reuses a supported shell-like managed pane for a new shell command on the same handle.
- [x] 2.2 Keep replacement scoped to supported shell panes and fail fast elsewhere.

## 3. Verification And Docs

- [x] 3.1 Add targeted tests plus MCP/router coverage for the new helpers.
- [x] 3.2 Update docs and backlog notes to describe the scoped takeover/replacement behavior.
